import { ProjectsLibrary } from "@/components/projects/projects-library";

export const metadata = {
  title: "Projects — Meshlab",
};

export default function ProjectsPage() {
  return <ProjectsLibrary />;
}
