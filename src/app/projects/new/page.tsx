import { NewProjectWizard } from "@/components/projects/new-project-wizard";

export const metadata = {
  title: "New project — Meshlab",
};

export default function NewProjectPage() {
  return <NewProjectWizard />;
}
